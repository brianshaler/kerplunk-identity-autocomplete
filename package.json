{
  "name": "kerplunk-identity-autocomplete",
  "displayName": "Contact Autocomplete",
  "description": "An autocomplete component that searches for contacts while you type.",
  "version": "0.0.1",
  "kerplunk": {
    "permissions": {
      "kerplunk-identity": {
        "models": [
          "Identity"
        ]
      }
    }
  },
  "keywords": [
    "kerplunk-plugin",
    "kp:Utility"
  ],
  "engines": {
    "node": ">=0.10"
  },
  "main": "./lib/index.js",
  "dependencies": {
    "lodash": "^3.10.0",
    "react": "^0.13.3"
  },
  "devDependencies": {
    "coffee-script": "^1.9.1",
    "glut": "0.0.1",
    "glut-coffee-amdify": "0.0.0",
    "gulp": "^3.8.8",
    "gulp-coffee": "^2.3.1"
  },
  "scripts": {
    "prepublish": "rm -rf ./lib ./public && gulp"
  }
}
